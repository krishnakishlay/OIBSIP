module.exports = {
  parser: 'babel-eslint', // Remove this line if it's not in dependencies
  extends: ['eslint:recommended', 'plugin:react/recommended'],
  plugins: ['react'],
  rules: {
    // Place to specify ESLint rules.
    // e.g. 'react/hooks/rules-of-hooks': 'error', // Checks rules of Hooks
  },
};